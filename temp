package AoCyy

import (
	"AoC/utils/date"
	"fmt"
)

func AoCdd() {
    year := yyyy
    day := dd
	fmt.Println("On " + date.DateStringForDay(year, day) + ":")
	//input := io.ReadInput(dd, yyyy)
	fmt.Println("Part 1:")
	// requests.SendAnswer(day, year, 0, 1)
	fmt.Println("Part 2:")
	// requests.SendAnswer(day, year, 0, 2)
}
